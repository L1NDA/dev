Monday
  Get the basic layout done (table)
    To do: clock, unsplash creds, 4 leafs

Tuesday
  CSS + Database
    To do: Favicon, database

Wednesday
  Finish database + chrome packaging?

Thursday
  Presentation practice + figure out chrome 

FriYay!
  Yay!
  
To do:
-bullet points --> objects
-time
-credits

Stretch goals:
-tabs
-enter block highlight deleting


https://zpl.io/2kP2c0


<script src="https://www.gstatic.com/firebasejs/4.1.2/firebase.js"></script>
<script>
  // Initialize Firebase
  var config = {
    apiKey: "AIzaSyA_hfKDMpBi4ot-VGBnBoIwHenERTIwOl0",
    authDomain: "div-1c0c2.firebaseapp.com",
    databaseURL: "https://div-1c0c2.firebaseio.com",
    projectId: "div-1c0c2",
    storageBucket: "",
    messagingSenderId: "180264721425"
  };
  firebase.initializeApp(config);
</script>

Questions
1. Unsplash load
3. Debounce (expected a function)
4. https://forismatic.com/en/api/


Unsplash backgrounds:
786665
252265 pastel
540595 warm & muted



<div className="title important">{leaf.titleText}</div>
                  : [
                      (leaf.titleText === 'urgent'
                      ? <div className="title important">{leaf.titleText}</div>
                      : null
                      )
                    ]
                    
                    
                    {leaf.hasTitle ? 
                  [
                    (leaf.hasTitleSide ? <div className="title important">{leaf.titleTextSide}</div>
                    : <div className="title important">{leaf.titleText}</div>
                    )
                    
                  ]
                  : null
                }
                
                
  {leaf.hasTitleSide 
                ? (<div className="title important">{leaf.titleText}</div>,
                  <div className="title important">{leaf.titleTextSide}</div>,
                  console.log('third item'),
                  console.log('fourth item'),
                  console.log('fifth item')
                  
                   )
                : [
                    (leaf.hasTitle
                      ? <div className="title important">{leaf.titleText}</div>
                      : null
                    )
                  ]
                 
               }
               
               
  // { if (leaf.hasTitleSide) { 
                   
              //     <div classname="title important">{leaf.titleText}</div>,
              //     <div className="title important">{leaf.titleTextSide}</div>
                  
              //     } else if (leaf.hasTitle) {
              //       <div className="title important">{leaf.titleText}</div>
              //     } else {
              //       null
              //     }
               
              // }
              
    /*if (cookie.load('userId') === undefined) {
      id = uuidv4();
      cookie.save('userId', id, { path: '/' });
      console.log("no id: " + id);
        this.state = {
          userId: id,
          leafs: [
            {
              leaf: 'one',
              note: '',
              hasTitle: true,
              hasTitleSide: true,
              titleText: 'important',
              titleTextSide: 'urgent'
            },
            {
              leaf: 'two',
              note: '',
              hasTitle: true,
              titleText: 'not important',
            },
            {
              leaf: 'three',
              note: '',
              hasTitle: true,
              titleText: 'not urgent'
            },
            {
              leaf: 'four',
              note: ''
            },
          ]
        };
        
    } else {*/
    
    
    
                  
                  
                   )
                : [
                    (leaf.hasTitle
                      ? (
                        leaf.leaf === "three"
                        ? <div className={`title vertical-titles ${leaf.titleText.replace(/\s/g,'')}`}>{leaf.titleText}</div>
                        : 
                        )
                      : null
                    )
                  ]
                 
               }
    
    
Questions:
-Table: ternary only returns one object?